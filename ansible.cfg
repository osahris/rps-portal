[defaults]
nocows = 1
stdout_callback = yaml

interpreter_python = auto_silent

library = /usr/share/ansible/library


retry_files_enabled = False
force_handlers = True

inventory_ignore_extensions = ~, .orig, .bak, .ini, .cfg, .retry, .pyc, .pyo, Vagrantfile

[ssh_connection]
pipelining = True
