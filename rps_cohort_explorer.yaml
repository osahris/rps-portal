# Ansible playbook for deploying a Flask app
---

- hosts: reverse_proxies
  remote_user: root
  roles:
    - role: reverse_proxy
      reverse_proxy_for_hosts: "{{ groups.rps_cohort_explorer }}"

- hosts: rps_cohort_explorer
  remote_user: root
  roles:
    - role: rps_cohort_explorer
