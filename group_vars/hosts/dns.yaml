dns_hosts: # TODO create an entry for all hosts for which a reverse_proxy_server_name is set
  - name: "{{ keycloak_server_name }}"
    ipv4: "{{ lxd_proxy_ip }}"
  - name: "{{ alerta_server_name }}"
    ipv4: "{{ lxd_proxy_ip }}"
# this file is used for additional private dns setup and
# must be accessible from the snap confinement
lxd_hosts_file:  /var/snap/lxd/common/lxd/networks/internal-hosts
