---

- hosts: localhost
  vars_files:
    - vars/globals.yaml
    - vars/client_config.yaml
  tasks:
    - name: write project config file
      copy:
        content: "{{client_config|to_yaml}}"
        dest: configs/config.{{project_name}}.yaml
