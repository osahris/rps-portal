---

- hosts: localhost
  vars:
    client_config:
      keycloak_management_url: https://testaccount.idcohorts.net/auth/
      keycloak_management_username: "{{keycloak_management_username}}"
      keycloak_management_username: "{{keycloak_management_password}}"
      keycloak_management_realm: master
      keycloak_realm: users
      discourse_url: https://test-discourse.idcohorts.net/
      discourse_username: system
      discourse_api_key: "{{ discourse_api_key }}"

  tasks:
    - name: write project config file
      copy:
        content:
