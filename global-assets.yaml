---

- hosts: wordpress_servers
  remote_user: root
  vars_files:
    - vars/globals.yaml
    - vars/secrets.yaml
  tasks:
    - name: clone global-assets repo
      git:
        repo: https://{{gitlab_deploy_token}}@gitlab.com/research-project-suite/global-assets.git
        dest: /var/www/testwebseite/global-assets
        version: master
