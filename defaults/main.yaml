---

discourse_hostname: "{{ inventory_hostname }}"
discourse_mail_domain: "{{ inventory_hostname }}"

locales:
  lang: de_DE.UTF-8
  language: de

discourse_openid_client_secret: secret
discourse_keycloak_management_username: admin
discourse_keycloak_management_password: password

discourse_postgres_db_default_text_search_config: pg_catalog.german

discourse_smtp_port: 587

discourse_mail_domain: "{{discourse_hostname}}"

discourse_web_ratelimited: true
