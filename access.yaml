---

- hosts: all
  remote_user: root
  vars_files:
    - vars/globals.yaml
    - vars/access.yaml
  tasks:
    - name: ssh authorized keys
      ansible.posix.authorized_key:
        user: root
        key: "{{ssh_authorized_keys}}"
