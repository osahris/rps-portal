---
- hosts: rps_servers
  become: true

  pre_tasks:
    - block:
        - name: Display current host
          debug:
            msg:
              - "{{ ansible_ssh_host }}"
              - "{{ inventory_hostname }}"

  roles:
    - role: rps_full_package

    # - role: static_header_test
    #   tags: static_header_test, static-header-test, core
    # - role: minimum_viable_product
    #   tags: minimum_viable_product, minimum-viable-product, mvp
    # - role: wordpress
    #   tags: wordpress
    # - role: webserver
    #   tags: webserver
