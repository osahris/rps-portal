---
typesense_project_name: "{{typesense_service_name}}"
typesense_project_directory: /srv/typesense/{{typesense_project_name}}
typesense_oauth_proxy_collection_configuration:
  proxy_collections:
    num_users:
      allowed_groups:
        - approved_users
