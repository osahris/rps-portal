---
remote_path: "/app/{{wiki_js_service_name}}"

# # ------------------------------------------ #
wiki_DB_NAME: "wiki"
wiki_DB_USER: "wikijs"
wiki_DB_PASS: "qyO%RE*0h^q0BjVhmrzqqi#OR@3dh7ax"

app_port: "3000"

# For each new running service add below a traefik router and a traefik service
traefik_dynamic_config:
  http:
    routers:
      wiki-js:
        rule: "Host(`{{wiki_js_service_name}}`)"
        entrypoints: websecure
        tls:
          certresolver: letsencrypt
        service: wiki-js

    services:
      wiki-js:
        loadBalancer:
          servers:
            - url: "http://{{wiki_js_service_name|replace('.','')}}_wiki_1:{{app_port}}" # "http://container_name:container_port"
