---
# - name: install nextcloud sociallogin app 
#   when: _nextcloud_status_task is not failed
#   community.docker.docker_container_exec:
#     container: "{{project_name}}_app"
#     user: www-data
#     command:
#       php occ app:install sociallogin

# - name: install nextcloud sociallogin app 
#   when: _nextcloud_status_task is not failed
#   community.docker.docker_container_exec:
#     container: "{{project_name}}_app"
#     user: www-data
#     command: 
#       php occ config:app:set sociallogin custom_providers --value="custom_oidc": [{"name": "keycloak", "title": "Keycloak", "authorizeUrl": "https://{{keycloak_domain}}/realms/{{keycloak_realm}}/protocol/openid-connect/auth","tokenUrl": "https://{{keycloak_domain}}/realms/{{keycloak_realm}}/protocol/openid-connect/token", "userInfoUrl": "https://{{keycloak_domain}}/realms/{{keycloak_realm}}/protocol/openid-connect/userinfo","logoutUrl": "", "clientId": "{{keycloak_my_application_id}}", "clientSecret": "{{keycloak_my_super_secret_secret}}", "scope": "openid", "groupsClaim": "groups", "style": "keycloak", "defaultGroup": ""}]}'
