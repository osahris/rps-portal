---
- import_tasks: secrets.yaml

- name: debian requirements
  apt:
    pkg:
      - jq

- include_tasks: per_env.yml
  vars:
    remote_path: "/app/{{ lookup('ansible.builtin.vars', 'wordpress_' + env + '_service_name') }}"
  loop:
    - prod
    - staging
  loop_control:
    loop_var: env
