MYSQL_DATABASE=wordpress
MYSQL_USER=wordpress
MYSQL_PASSWORD={{ lookup('ansible.builtin.vars', 'wordpress_' + env + '_mysql_password') }}
MYSQL_ROOT_PASSWORD={{ lookup('ansible.builtin.vars', 'wordpress_' + env + '_mysql_root_password') }}
