---
version: "3"

networks:
  proxy:
    external: true


services:

  rps_people: # Service name
    build:
      context: ./
      dockerfile: Dockerfile
    restart: unless-stopped
    logging:
      options:
        max-size: 5m
    networks:
      - proxy
    # ports:
    #   - "80:80"
    env_file: 
      - "./.env"
    # volumes:
    #   - db:/app/matchmaking/db
