---
version: "3"

networks:
  proxy:
    external: true

services:

  flask: # Service name
    build:
      context: ./
      dockerfile: Dockerfile
    logging:
      options:
        max-size: 5m
    restart: unless-stopped
    networks:
      - proxy
