---
keycloak_groups_auth_keycloak_url: "https://{{ keycloak_service_name }}"
keycloak_groups_auth_client_id: admin-cli
keycloak_groups_auth_realm: "{{keycloak_management_realm|default('master')}}"
keycloak_groups_auth_username: admin
keycloak_groups_auth_password: "{{keycloak_admin_password}}"

keycloak_groups_realm: "{{keycloak_realm}}"
