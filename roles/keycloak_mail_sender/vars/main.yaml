---
keycloak_approval_email_sender_secrets:
  keycloak_approval_email_sender_keycloak_client_secret: "{{ keycloak_approval_email_sender_keycloak_client_secret | default(lookup('ansible.builtin.password', '/dev/null length=32 chars=ascii_letters,digits')) }}"
  keycloak_registration_mail_server_config_password: "{{ keycloak_registration_mail_server_config_password | default(false) }}"

sync_path: "/srv/{{keycloak_approval_email_sender_keycloak_client_client_id}}"
