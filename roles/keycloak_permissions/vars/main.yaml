---
keycloak_permissions_secrets:
  permission_sync_keycloak_client_secret: "{{ permission_sync_keycloak_client_secret | default(lookup('ansible.builtin.password', '/dev/null length=32 chars=ascii_letters,digits')) }}"

sync_path: "/srv/{{permission_sync_keycloak_client_client_id}}"
