---
# Ansible variables
remote_path: "/app/{{rps_idia_service_name}}"
app_port: "80"

# For each new running service add below a traefik router and a traefik service
traefik_dynamic_config:
  http:
    routers:
      rps_idia:
        rule: "Host(`{{rps_idia_service_name}}`)"
        entrypoints: websecure
        middlewares:
        - header-redirect
        tls:
          certresolver: letsencrypt
          domains:
            - main: "{{rps_idia_service_name}}"
        service: rps_idia

    services:
      rps_idia:
        loadBalancer:
          servers:
            - url: "http://{{rps_idia_service_name|replace('.', '')}}_idia_1:{{app_port}}" # "http://container_name:container_port"


    # middlewares:
    #   oauth2-proxy-auth-redirect:
    #     forwardAuth:
    #       address: http://oauth2-proxy:4180/
    #       trustForwardHeader: true
    #       authResponseHeaders:
    #         - X-Auth-Request-Access-Token
    #         - Authorization
    #         - X-Auth-Request-Groups
