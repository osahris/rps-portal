---
version: "3"

networks:
  proxy:
    external: true

services:
  static-website:
    image: nginx
    volumes:
      - ./html:/usr/share/nginx/html
    restart: unless-stopped
    networks:
      - proxy
