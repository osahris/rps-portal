---

- hosts: reverse_proxies
  remote_user: root
  roles:
    - role: reverse_proxy
      reverse_proxy_for_hosts: "{{ groups.rps_header_servers }}"

- hosts: rps_header_servers
  remote_user: root
  roles:
    - role: rps_header_server
