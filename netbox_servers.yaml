---

- hosts: reverse_proxies
  remote_user: root
  roles:
    - role: reverse_proxy
      reverse_proxy_for_hosts: "{{ groups.netbox_servers }}"

- hosts: netbox_servers
  remote_user: root

  roles:
    - role: netbox
